{
  "name": "validation-1170-sqlite-p3-init",
  "version": "0.0.0",
  "private": true,
  "description": "Isolated P3 prototype: exclusive explicit-init initializer + synthetic pre-existing states + race child for a conditional-admission SQLite store (task 1170). Experiment only - not a product package, adopted by nothing, imported by nothing.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-reporter=tap --test-reporter-destination=stdout init-exclusive.test.cjs"
  },
  "dependencies": {
    "better-sqlite3": "13.0.3"
  }
}
